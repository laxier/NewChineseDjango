meta:
  environment: python
  toolchain:
    name: pip
    version: "3.8"
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: "80"
  command: sudo gunicorn --workers 3 --bind 0.0.0.0:80 chinese_learning_platform.chinese_learning_platform.wsgi:application

